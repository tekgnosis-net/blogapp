services:
  web:
    image: ghcr.io/tekgnosis-net/blogapp:latest
    pull_policy: always
    ports:
      - "8080:5000"
    environment:
      - PORT=5000
      - DEBUG=True
      - DATABASE_URI=mysql+mysqldb://root:rootpassword@db:3306/codingthunder
    depends_on:
      - db
    restart: unless-stopped

  db:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: codingthunder
      MYSQL_USER: bloguser
      MYSQL_PASSWORD: blogpassword
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mysql_data:
